services:
  mongo:
    image: mongo:7
    container_name: cqrs-query-mongo
    ports:
      # Host port 27018 to avoid clashing with a local MongoDB on 27017.
      - "27018:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
